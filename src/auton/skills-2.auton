/****
- [ ] shoot high flag
- [ ] face cap
- [ ] pick up ball & flip cap
- [ ] backup, turn & shoot mid flag
- [ ] toggle low flag
- [ ] backup & pick up ball & flip cap
- [ ] turn, shoot mid flag
- [ ] toggle low flag
****/

posTracker.setPos({ 0, 1 }, 90);
// FLYWHEEL_SET(FLYWHEEL_IDLE);

// robot.combineSet(false);

robot.moveTo({ 0, 18 }, { 10, 0, 0, 60 }, { 1, 0, 0, 60, 0 });
